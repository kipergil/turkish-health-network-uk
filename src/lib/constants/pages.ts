/**
 * Slugs for CMS pages (`pages` collection) that the app links to directly
 * (nav items, redirects, etc), kept in one place so the nav constant and the
 * Directus seed script can't drift apart.
 */
export const NHS_BENEFITS_GUIDE_SLUG = "nhs-insurance-benefits-uk";
